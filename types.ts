export interface ServiceItem {
  title: string;
  description?: string;
  price?: string;
}

export interface Testimonial {
  name: string;
  review: string;
  rating: number;
}

export interface NavItem {
  label: string;
  href: string;
}